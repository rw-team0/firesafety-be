<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rayworld.firesafety.facility.mapper.PanelMapper">
    <resultMap id="panelResultMap" type="com.rayworld.firesafety.facility.model.Panel">
        <id property="panelId" column="panel_id"/>
        <result property="siteId" column="site_id"/>
        <result property="name" column="name"/>
        <result property="deviceSerial" column="device_serial"/>
        <result property="mNo" column="m_no"/>
        <result property="installedAt" column="installed_at"/>
        <result property="status" column="status"/>
        <result property="isOnline" column="is_online"/>
        <result property="lastCommunicatedAt" column="last_communicated_at"/>
        <result property="circuitCount" column="circuit_count"/>
        <result property="leakMaThreshold" column="leak_ma_threshold"/>
        <result property="tempThreshold" column="temp_threshold"/>
        <result property="humidityThreshold" column="humidity_threshold"/>
        <result property="overcurrentThreshold" column="overcurrent_threshold"/>
        <result property="gasThreshold" column="gas_threshold"/>
        <result property="fireThreshold" column="fire_threshold"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <!-- 현장 아래 신규 분전반 등록 -->
    <insert id="insertPanel"
            parameterType="com.rayworld.firesafety.facility.model.Panel"
            useGeneratedKeys="true"
            keyProperty="panelId">
        INSERT INTO panel (
            site_id,
            name,
            device_serial,
            m_no,
            installed_at,
            status,
            is_online,
            circuit_count,
            leak_ma_threshold,
            temp_threshold,
            humidity_threshold,
            overcurrent_threshold,
            gas_threshold,
            fire_threshold
        ) VALUES (
            #{siteId},
            #{name},
            #{deviceSerial},
            #{mNo},
            #{installedAt},
            #{status},
            #{isOnline},
            #{circuitCount},
            #{leakMaThreshold},
            #{tempThreshold},
            #{humidityThreshold},
            #{overcurrentThreshold},
            #{gasThreshold},
            #{fireThreshold}
        )
    </insert>

    <!-- 등록/수정/삭제 대상 분전반 활성 여부 확인 -->
    <select id="findActivePanelById" resultMap="panelResultMap">
        SELECT
            p.panel_id,
            p.site_id,
            p.name,
            p.device_serial,
            p.m_no,
            p.installed_at,
            p.status,
            p.is_online,
            p.last_communicated_at,
            p.circuit_count,
            p.leak_ma_threshold,
            p.temp_threshold,
            p.humidity_threshold,
            p.overcurrent_threshold,
            p.gas_threshold,
            p.fire_threshold,
            p.created_at,
            p.updated_at,
            p.deleted_at
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        WHERE p.panel_id = #{panelId}
          AND p.deleted_at IS NULL
          AND s.deleted_at IS NULL
    </select>

    <!-- SUPER_ADMIN은 전체 활성 분전반 조회 -->
    <select id="findActivePanels" resultMap="panelResultMap">
        SELECT
            p.panel_id,
            p.site_id,
            p.name,
            p.device_serial,
            p.m_no,
            p.installed_at,
            p.status,
            p.is_online,
            p.last_communicated_at,
            p.circuit_count,
            p.leak_ma_threshold,
            p.temp_threshold,
            p.humidity_threshold,
            p.overcurrent_threshold,
            p.gas_threshold,
            p.fire_threshold,
            p.created_at,
            p.updated_at,
            p.deleted_at
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        WHERE p.deleted_at IS NULL
          AND s.deleted_at IS NULL
        <if test="siteId != null">
          AND p.site_id = #{siteId}
        </if>
        <if test="status != null">
          AND p.status = #{status}
        </if>
        ORDER BY p.panel_id DESC
    </select>

    <!-- ADMIN/GENERAL은 배정 현장의 활성 분전반만 조회 -->
    <select id="findActivePanelsByUserId" resultMap="panelResultMap">
        SELECT
            p.panel_id,
            p.site_id,
            p.name,
            p.device_serial,
            p.m_no,
            p.installed_at,
            p.status,
            p.is_online,
            p.last_communicated_at,
            p.circuit_count,
            p.leak_ma_threshold,
            p.temp_threshold,
            p.humidity_threshold,
            p.overcurrent_threshold,
            p.gas_threshold,
            p.fire_threshold,
            p.created_at,
            p.updated_at,
            p.deleted_at
        FROM panel p
        INNER JOIN site s ON s.site_id = p.site_id
        INNER JOIN user_site us ON us.site_id = s.site_id
        WHERE us.user_id = #{userId}
          AND us.deleted_at IS NULL
          AND p.deleted_at IS NULL
          AND s.deleted_at IS NULL
        <if test="siteId != null">
          AND p.site_id = #{siteId}
        </if>
        <if test="status != null">
          AND p.status = #{status}
        </if>
        ORDER BY p.panel_id DESC
    </select>

    <!-- 장비 시리얼 중복 등록 방지 -->
    <select id="existsPanelByDeviceSerial" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM panel
            WHERE device_serial = #{deviceSerial}
        )
    </select>

    <!-- 수정 시 자기 자신을 제외한 장비 시리얼 중복 방지 -->
    <select id="existsPanelByDeviceSerialExceptSelf" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM panel
            WHERE device_serial = #{deviceSerial}
              AND panel_id != #{panelId}
        )
    </select>

    <!-- 관리자 분전반 기본 정보와 임계치 수정 -->
    <update id="updatePanel" parameterType="com.rayworld.firesafety.facility.model.Panel">
        UPDATE panel
        SET name = #{name},
            device_serial = #{deviceSerial},
            m_no = #{mNo},
            installed_at = #{installedAt},
            circuit_count = #{circuitCount},
            leak_ma_threshold = #{leakMaThreshold},
            temp_threshold = #{tempThreshold},
            humidity_threshold = #{humidityThreshold},
            overcurrent_threshold = #{overcurrentThreshold},
            gas_threshold = #{gasThreshold},
            fire_threshold = #{fireThreshold},
            updated_at = CURRENT_TIMESTAMP
        WHERE panel_id = #{panelId}
          AND deleted_at IS NULL
    </update>

    <!-- 분전반 row는 보존하고 일반 조회 대상에서 제외 -->
    <update id="softDeletePanel">
        UPDATE panel
        SET deleted_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE panel_id = #{panelId}
          AND deleted_at IS NULL
    </update>
</mapper>
